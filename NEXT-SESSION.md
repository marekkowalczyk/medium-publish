# Next Session

## New

- Consider `--dry-run` flag: validate and upload images but don't create the post
- Consider `make test` target in Makefile for consistency with `make install`
- The `[Unreleased]` CHANGELOG section has README-only changes; roll into v1.4.0 when there's something substantive
